#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged for code formatting only
echo "📝 Formatting code with Prettier..."
npx lint-staged

echo "✅ Pre-commit checks passed!"
