// Database utilities - now using Convex only

export const db = {
  // Placeholder for database operations
  async query(sql: string) {
    console.warn('Database query not implemented:', sql);
    return [];
  },
};
